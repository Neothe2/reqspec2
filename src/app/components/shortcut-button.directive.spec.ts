import { ShortcutButtonDirective } from './shortcut-button.directive';

describe('ShortcutButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortcutButtonDirective();
    expect(directive).toBeTruthy();
  });
});
